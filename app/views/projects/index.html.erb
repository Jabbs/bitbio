
<div class="row">
  <div class="span9">
    <% if @projects && !@projects.empty? %>
      <% @projects.each do |project| %>
        <%= render 'project', project: project %>
      <% end %>
    <% else %>
      <h3><em>n/a</em></h3>
    <% end %>
  </div>
</div>
<%= will_paginate %>

