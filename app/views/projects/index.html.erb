
<div class="row">
  <div class="span9">
    <% if @projects.empty? %>
      <h3>No projects found.</h3>
    <% else %>
      <% @projects.each do |project| %>
        <%= render 'project', project: project %>
      <% end %>
    <% end %>
  </div>
</div>

