<% if !ENV['STAGING'].present? || Rails.env.development? %>
  <% content_for :title do %>
    <%= t('application.root.title_with_tag_line', tag_line: "Tags") %>
  <% end %>
  <% content_for :description do %>
    Search for scientific services or projects by tags.
  <% end %>
<% end %>

<div class="row">
  <div class="span9 pull-right">
    <h2>Project tags</h2>
    <i class="icon-tags"></i>:
    <%= raw Tag.top_project_tags.map { |t| link_to "#{t.name} (#{t.projects.size})", tag_projects_path(t.name) }.join(', ') %>
  </div>
</div>

