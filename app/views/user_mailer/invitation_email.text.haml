= link_to @user.full_name, user_url(@user)
is inviting you to join BitBio's Science Community, Marketplace, and Resource

= link_to "Join BitBio", root_url + "?invite_token=#{@user.invite_token}", class: "btn"
