
<div class="span9 pull-right">
  <% if current_user %>
  <div style="height: 0px;"><%= link_to "<i class='icon-plus'></i> ADD SERVICE".html_safe, new_service_path, class: 'my-button-blue',
  style: 'position:relative; left: 583px; top: -38px; z-index: 12;' %></div>
  <% end %>
  <% @services.each do |service| %>
    <%= render 'service', service: service %>
  <% end %>
</div>
<div class="span9 pull-right"><%= will_paginate @projects %></div>